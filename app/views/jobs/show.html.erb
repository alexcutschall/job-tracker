Title: <%= @job.title %>
<br>

<% if @job.category != nil %>
  Category: <%= @job.category.title %><br>
<% end %>

Description: <%= @job.description %>
<br>

Level of Interest: <%= @job.level_of_interest %>
<br>

City: <%= @job.city %>
<br>

Company: <%= link_to @job.company.name, company_path(@job.company.id) %>

Notes:<br />

<%= render partial: "job_comments/form" %>

<% @job.comments_ordered.each do |comment| %>
  <%= comment.created_at %>
  <%= comment.body %><br>
<% end %>

<br /><%= link_to "Edit", edit_company_job_path(@job.company, @job) %>
<%= link_to "Delete", job_path(@job), method: "delete",
data: {confirm:"Really delete this job?"} %>
